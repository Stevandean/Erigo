import { FC } from "react";

import ContainerProfileSettings from "@/components/Containers/ProfileSettings/ProfileSettings";

const ProfileSettings: FC = () => {
  return <ContainerProfileSettings />;
};

export default ProfileSettings;
